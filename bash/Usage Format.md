This usage comment format is clean and easy to read. It’s a valuable inclusion in any script with multiple options.

```sh
# Usage:
#   command [ --help ]
#           [ --option "..." ]
#           [ --someOtherOption "..." ]
#           [ --version ]
```